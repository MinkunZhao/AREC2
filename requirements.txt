# AREC2 dependencies
#
# AutoDL note:
# - Prefer an AutoDL image with CUDA + PyTorch already installed.
# - If the image already has a compatible torch build, pip will keep it.
# - If you need a specific CUDA build, install torch from the PyTorch/AutoDL
#   source first, then run: pip install -r requirements.txt

# Core deep-learning stack
torch>=2.1.0
transformers>=4.51.0,<5.0.0
accelerate>=0.30.0
peft>=0.11.0
trl>=0.12.0,<0.25.0
datasets>=2.14.0
huggingface-hub>=0.24.0
safetensors>=0.4.3
tokenizers>=0.20.0
sentencepiece>=0.2.0
protobuf>=4.25.0

# Data processing and retrieval
numpy>=1.24.0
pandas>=2.0.0
pyarrow>=14.0.0
scipy>=1.10.0
scikit-learn>=1.3.0
rank-bm25>=0.2.2
jieba>=0.42.1

# Config, logging, progress, and utilities
pyyaml>=6.0
tqdm>=4.64.0
pydantic>=2.0.0
tenacity>=8.2.0
diskcache>=5.6.0
openai>=1.30.0

# Optional but useful on AutoDL / remote training jobs
tensorboard>=2.14.0

# Development and smoke tests
pytest>=7.4.0
